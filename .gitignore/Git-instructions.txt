d:
cd 22_06 doc\T-Git\1
git init

git config --global -l
git status
git add .
git status
git commit -m "Commit Inicial"

#verificar cambios:
>git status
>git diff
#regresar cambios:
>git checkout .

#confirmar cambios:
>git add.
>git commit -m "Se modifico el titulo"

#visualizar el historico de cambios:
>git log

#valida solo un cambio
modified: index.html
>git add index.html
>git commit -m "Se modifico index.html la palabra Coder"

js/validation.js
>git add js/
>git status
new file: js/validacion.js

#
>git add css/*.css
>git status
#
>git reset css/*.scss
>git status
#ya no esta controlando el archivo scss

>git reset *.scss
>git status

>git add --all
>git status
>git commit -m "Se agrego contenido a la carpeta"

>git log
>ENTER #continua
>q #termina

#para conocer la información de los comandos
>git help commit
>git help push

#Alias
>git config --global alias.l "log"
>git l
>git config --global alias.s "status"
>git s

#corregir el tag de un commit
>git commit -m "Nueva modificación al ndex"
>git commit --amend -m "Nueva modificación al index"

#cambiar el nombre de un archivo
>git mv nombreacambiar.html nombrecambiado.html

#eliminar un archivo
>git rm nombrecambiado.html
#recuperar archivo
>git reset --soft 8fba101195821........etc#un paso atras pero no recupera el archivo
>git reset --hard id...................etc#recupera el archivo

#el historico completo asi hubieramos revertido los cambios
>git reflog
